---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar";
import Footer from "../components/Footer.astro";
import TextReveal from "../components/TextReveal";
import { Image } from "astro:assets";
import profileImage from "../images/profile-img/luciapuccioprofile.jpeg";
---

<Layout title="Sobre Mí | Lucia Puccio">
  <Navbar client:only="react" />

  <main class="min-h-screen bg-stone-50 overflow-hidden relative">
    <!-- Organic Background Elements (Floating Blobs) -->
    <div
      class="absolute top-0 right-0 w-[500px] h-[500px] bg-sky-100/40 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2"
    >
    </div>
    <div
      class="absolute bottom-0 left-0 w-[600px] h-[600px] bg-stone-200/40 rounded-full blur-3xl translate-y-1/2 -translate-x-1/3"
    >
    </div>

    <div
      class="container mx-auto px-4 py-32 md:py-40 relative z-10 flex flex-col md:flex-row items-center gap-12 md:gap-20"
    >
      <!-- Profile Image with Organic Mask -->
      <div class="w-full md:w-1/2 flex justify-center md:justify-end relative">
        <div class="relative w-full max-w-md aspect-[4/5]">
          <!-- Artistic border/frame -->
          <div
            class="absolute inset-0 border border-stone-300 rounded-[2rem] translate-x-4 translate-y-4"
          >
          </div>
          <div class="relative rounded-[2rem] overflow-hidden shadow-xl">
            <Image
              src={profileImage}
              alt="Lucia Puccio"
              width={600}
              quality="high"
              class="w-full h-full object-cover grayscale hover:grayscale-0 transition-all duration-700"
            />
            {/* Soft overlay gradient */}
            <div
              class="absolute inset-0 bg-gradient-to-t from-stone-900/20 to-transparent pointer-events-none"
            >
            </div>
          </div>
        </div>
      </div>

      <!-- Text Content -->
      <div class="w-full md:w-1/2 text-left space-y-8">
        <h1
          class="font-cursive text-6xl md:text-7xl text-stone-800 leading-tight"
        >
          <TextReveal text="Lucia" delay={0.2} client:load />
          <span class="block ml-12">
            <TextReveal text="Puccio" delay={0.4} client:load />
          </span>
        </h1>

        <div class="w-20 h-[1px] bg-stone-800/30"></div>

        <div
          class="space-y-6 text-lg font-light text-stone-600 leading-relaxed font-sans max-w-lg"
        >
          <p>
            Hola, soy Lucía. Llevo más de una década deteniendo el tiempo a
            través de mi lente.
          </p>
          <p>
            Mi viaje me ha llevado por el mundo, buscando siempre esa conexión
            invisible entre el arte, la naturaleza imperceptible y la esencia
            humana.
          </p>
          <p class="text-stone-800 italic">
            "No solo capturo imágenes; narro historias silenciosas donde la luz
            y la emoción se encuentran."
          </p>
        </div>

        <div class="pt-8">
          <img
            src="/signature.png"
            alt="Firma"
            class="h-12 opacity-50 hidden"
          />
          {
            /* Fallback signature in font if image unique signature not available */
          }
          <p class="font-cursive text-3xl text-stone-400 rotate-[-5deg]">
            Lucia P.
          </p>
        </div>
      </div>
    </div>
  </main>

  <Footer />
</Layout>
